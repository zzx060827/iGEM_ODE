# Archive

This directory preserves superseded scripts for provenance. Files under
`legacy/` are not called by the current model or frontend pipeline and may use
older equations, output paths or parameter assumptions.

For new work, use the entry points under `model/`. A historical script should
only be restored after its equations and units have been compared with the
current implementation.
